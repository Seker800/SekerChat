<ui:FluentWindow x:Class="SekerChat.DesktopPet.SettingsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="服务器地址"
                 Width="520"
                 Height="170"
                 MinHeight="170"
                 MaxHeight="170"
                 ResizeMode="NoResize"
                 Background="{StaticResource AppBackgroundBrush}"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen">
  <Grid Background="{StaticResource AppBackgroundBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="48" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ui:TitleBar Grid.Row="0"
                 Title="服务器地址"
                 ShowMaximize="False"
                 ShowMinimize="False"
                 Background="{StaticResource ChromeBackgroundBrush}"
                 ButtonsBackground="{StaticResource ChromeBackgroundBrush}"
                 ButtonsForeground="{StaticResource InkBrush}" />

    <Grid Grid.Row="1" Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="40" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBox x:Name="WebBaseUrlBox"
               Height="40"
               Padding="12,0"
               VerticalContentAlignment="Center" />
      <ui:Button Grid.Column="1"
                 Width="84"
                 Height="40"
                 Margin="12,0,0,0"
                 Appearance="Primary"
                 Content="保存"
                 Click="Save_Click" />

      <TextBlock x:Name="StatusText"
                 Grid.Row="1"
                 Grid.ColumnSpan="2"
                 Margin="2,8,2,0"
                 Foreground="#F23F43"
                 TextWrapping="Wrap"
                 Visibility="Collapsed" />
    </Grid>
  </Grid>
</ui:FluentWindow>
