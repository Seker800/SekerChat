# 00-idea.md

## 原始反馈

现有订阅中心把类型、标签、摘要、外部链接和全文卡片混在一起，信息层级重复且阅读体验不自然。正文不能使用 Markdown，也不能把上传图片插入正文。

## 修订方向

- 删除类型、摘要和外部链接字段。
- 保留标题、标签、Markdown 正文和附件。
- 标签通过回车、空格、中英文逗号创建胶囊。
- 列表只显示自动正文预览，点击进入完整详情。
- 上传图片后以稳定的 `attachment://<id>` 引用插入正文。
- 图片不增加专属大小或数量限制，与普通附件统一沿用现有规则。

