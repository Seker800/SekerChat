# Security policy

## Reporting a vulnerability

请优先使用 GitHub 仓库 `Security` 页面中的 **Report a vulnerability** 私密报告入口。
不要在公开 Issue、Discussion、PR、日志或截图中披露漏洞细节、访问令牌、连接串或用户数据。

报告应尽量包含：

- 受影响的版本或提交
- 可重复的最小步骤
- 实际影响和必要前置条件
- 建议修复方向（如有）

如果私密报告入口不可用，请先通过仓库所有者公开资料中列出的私密联系方式建立联系，
确认安全渠道后再发送敏感细节。

## Supported versions

安全修复优先应用于 `main` 和最新发布版本。更早版本通常需要先升级后再获得修复。
